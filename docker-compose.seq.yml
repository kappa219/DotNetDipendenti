services:
  seq:
    image: datalust/seq:latest
    ports:
      - "5341:5341"   #  servizi → Seq
      - "8081:80"     # apri localhost:8081
    environment:
      ACCEPT_EULA: "Y"
      SEQ_FIRSTRUN_ADMINPASSWORD: "Admin1234!"
    volumes:
      - seq-data:/data

volumes:
  seq-data: